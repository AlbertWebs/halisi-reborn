@props(['number', 'label'])

<div class="text-center">
    <div class="text-4xl font-serif font-bold text-[var(--color-forest-green)] mb-2">{{ $number }}</div>
    <div class="text-sm uppercase tracking-wide text-[var(--color-earth-brown)]">{{ $label }}</div>
</div>
